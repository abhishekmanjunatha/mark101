'use server'

// Placeholder — fully implemented in Phases 8 & 9
// All AI calls are server-side only — API keys never exposed to browser

export async function enhanceDocument(content: string, patientContext: Record<string, unknown>) {
  // Phase 8
}

export async function analyzeLabReport(fileUrl: string, patientContext: Record<string, unknown>) {
  // Phase 9
}
